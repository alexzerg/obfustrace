# Micro-Embassy Implementation Plan

1. Create a clean Next.js 16 TypeScript repository.
   - Done when `npm install` completes with zero vulnerabilities and route types generate.
2. Replace the starter page with a synthetic emergency-case dashboard.
   - Done when four recipient roles show distinct allowlisted and protected fields.
3. Add client-side revoke and reissue interactions for the demo.
   - Done when status and expiration change visibly without reload.
4. Add project metadata, accessible global styling, and product documentation.
   - Done when the title, description, README, and Devpost draft consistently use Micro-Embassy positioning.
5. Add automated checks.
   - Done when lint, production build, and Playwright core-flow tests exit 0.
